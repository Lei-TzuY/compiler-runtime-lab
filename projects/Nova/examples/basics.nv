fn choose(flag: Bool, left: Int, right: Int) -> Int {
    var selected: Int;
    if flag {
        selected = left;
        0
    } else {
        selected = right;
        0
    };

    selected + 1
}

fn main() -> Int {
    var seed: Int;
    seed = 41;
    choose(seed > 0 && true, seed, 0)
}
